Creates project CLI in deploy/flock.

`flock init` - creates `deploy`, `Flockfile`, etc.
`flock build` - calls `swift build` inside of `deploy/flock`
`flock <task>` - forwards to project CLI inside of `deploy/flock`